<em>: The <em> tag is used to define emphasized text. 
The content inside is typically displayed in italic.

New Line <br/>: the tag is used to make new line break on html page.

Paragraph <p>: defines a paragraph
Ex:  <p>This is some text in a paragraph. </p>
OR:  <p>This is some text in a paragraph.
Note: Close tag is optional.

sub <sub>: this tag Subscript text appears half a character below the normal line.

sup <sup>: this tag Superscript text appears half a character upper the normal line.

The <strong> tag is used to define text with strong importance. The content inside is typically displayed in bold.

<mark> — Highlighting.

Headings :
The h1 element can be used for headlines, or section headings. 
There are actually six of these heading elements: h1 through h6.
Note :
h1 is used for the main heading on a page, while the rest are used for subheadings. 
We use a smaller heading to show that one section is inside another section.
If one section simply comes after the other then we use the same level of heading.


Block and Inline:
-------------------
HTML Elements is divided in two category types :   Block Element, inline Element.
1- Block Elements: this type of elements the HTML Create invisible Box around Contact. 
This box object have Properties like Margins , height and width.
Ex: <DIV>, <OL>,<UL>,<P>,<H>,<video>, <table>

2- Inline : didn't surrounded with any kind of Box.
EX: <br> ,<em>, <strong>,<mark>, <a>, <span>,<img>.


<DIV> : is short for division, becuase it divide up the page into different sections.
<OL>: order list uses numbers to refer to its item list <il>.
<UL>: unorder list uses Dots to refer to its item list <il>.

Nested lists:

Cascading style sheet [CSS] : The language that used to manage the Appearance of the web page .
Document Object Model [DOM] : This is a Model that represent the page elements and each Childs  in a element's Tree Structure in the computer memory[RAM].
color :Change Font Color.
EX: color: blue

text-align: Locate text in the lineز
EX: text-align: center

What If Want to Apply Mulitpil Style to the same Element ?
we can add all style sprated with semicolon witch is optional if there is one Property that no need for it.
EX: <p style="color: blue; text-align: center;">Hello world!</p>

attribute value : the attribute value is the part that comes after the = sign,we put quotes around the value.
EX: <a href="https://www.udacity.com">Udacity</a>

the  CSS ruleset is made up of a selector and a declaration block.

Selector: Selectors indicate which HTML elements the ruleset will apply to.
Decorator : [Property,Value] For Each Element Or CLass Or ID on all Page.

EX: [Selector] il [Decorator]{Color :Red;}